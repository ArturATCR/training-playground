pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    environment {
        NUMBER_OF_ISSUES = 'mock'
    }
    stages {
        stage('notification') {
            steps {
                echo "$NUMBER_OF_ISSUES"
            }
        }
        stage('notification') {
            steps {
                echo "$NUMBER_OF_ISSUES"
            }
        }
    }
}