pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('create file with Date'){
            steps {
                retry(3) {
                    sh 'date > output.txt'
                }
            }
        }
    }
}